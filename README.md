Requires SFML 2.5 installed.

If the AS_LIBRARY is not set when cmake is run, main.cpp will be used for the application as a demo to show its usage. Otherwise set AS_LIBRARY in order to build your application on top of this.